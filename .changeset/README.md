# Changesets

Add a changeset for every user-visible package, protocol, or schema change. Protocol or schema breaking changes require compatibility notes and an explicit version change. Packages remain in a fixed version group through 1.x.
