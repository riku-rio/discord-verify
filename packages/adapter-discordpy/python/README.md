# Discord Verify Python adapter bridge

This source tree is embedded in the `@discord-verify/adapter-discordpy` npm package. It exists for adapter development and testing and is not intended for independent PyPI publication.
